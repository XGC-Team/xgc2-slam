name: ci

# This tree only aggregates the maintained Faster-LIO checkout.
# Unmaintained upstream forks live under temp/ and are not CI'd here.

on:
  workflow_dispatch:

jobs:
  aggregator-ci-disabled:
    name: Aggregator CI disabled
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "xgc2-slam does not build a multi-algorithm matrix."
          echo "Run CI on XGC-Team/xgc2-faster-lio."
